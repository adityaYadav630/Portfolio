<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Message Sent</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    *{
      margin:0;
      padding:0;
      box-sizing:border-box;
      font-family: 'Poppins', sans-serif;
    }

    body{
      height:100vh;
      width:100vw;
      display:flex;
      overflow:hidden;
      background:#0f0f0f;
    }

    /* LEFT */
    .left{
      flex:1;
      background:linear-gradient(135deg,#f5f5f5,#eaeaea);
      display:flex;
      align-items:center;
      justify-content:center;
      padding:3rem;
      animation: slideInLeft 1s ease forwards;
    }

    .content{
      max-width:450px;
      animation: fadeUp 1.2s ease forwards;
    }

    .content h1{
      font-size:3.2rem;
      font-weight:600;
      color:#111;
      margin-bottom:1rem;
    }

    .content h1 span{
      color:#8b1d1d;
    }

    .content p{
      font-size:1.15rem;
      color:#444;
      line-height:1.7;
    }

    .content a{
      display:inline-block;
      margin-top:2.2rem;
      padding:.9rem 2rem;
      background:#000;
      color:#fff;
      text-decoration:none;
      border-radius:40px;
      font-size:.95rem;
      letter-spacing:.5px;
      transition:.35s;
    }

    .content a:hover{
      background:#222;
      transform:translateY(-3px);
    }

    /* RIGHT */
.right{
  flex:1;
  background:radial-gradient(circle at top,#1a1a1a,#000);
  display:flex;
  align-items:center;
  justify-content:flex-start; /* center hata */
  padding-left:80px;          /* card left shift */
}


    .card{
      width:340px;
      height:460px;
      background:linear-gradient(145deg,#1f1f1f,#121212);
      border-radius:22px;
      display:flex;
      flex-direction:column;
      align-items:center;
      justify-content:center;
      color:#fff;
      box-shadow:0 30px 80px rgba(0,0,0,.6);
      animation: float 4s ease-in-out infinite;
      position:relative;
      overflow:hidden;
  transform: translateX(-80px);
    }

    .card::before{
      content:"";
      position:absolute;
      inset:-50%;
      background:conic-gradient(from 0deg, transparent, #8b1d1d, transparent);
      animation: rotate 6s linear infinite;
      opacity:.35;
    }

    .card-content{
      position:relative;
      z-index:2;
      text-align:center;
    }

.check{
  width:90px;
  height:90px;
  border-radius:50%;
  background:#8b1d1d;
  display:flex;
  align-items:center;
  justify-content:center;
  font-size:2.5rem;
  margin-bottom:1.2rem;
  margin-left: 80px;
}


    .card h2{
      font-size:1.5rem;
      margin-bottom:.5rem;
    }

    .card p{
      font-size:.95rem;
      color:#ccc;
      max-width:240px;
    }

    /* ANIMATIONS */
    @keyframes fadeUp{
      from{opacity:0; transform:translateY(25px);}
      to{opacity:1; transform:translateY(0);}
    }

    @keyframes slideInLeft{
      from{transform:translateX(-80px); opacity:0;}
      to{transform:translateX(0); opacity:1;}
    }

    @keyframes slideInRight{
      from{transform:translateX(80px); opacity:0;}
      to{transform:translateX(0); opacity:1;}
    }

    @keyframes float{
      0%,100%{transform:translateY(0);}
      50%{transform:translateY(-12px);}
    }

    @keyframes rotate{
      to{transform:rotate(360deg);}
    }

    @keyframes pop{
      from{transform:scale(0); opacity:0;}
      to{transform:scale(1); opacity:1;}
    }

    /* RESPONSIVE */
    @media(max-width:768px){
      body{
        flex-direction:column;
      }
      .right{
        display:none;
      }
      .content h1{
        font-size:2.4rem;
      }
    }
  </style>
</head>

<body>

  <div class="left">
    <div class="content">
      <h1>Message <span>Sent</span></h1>
      <p>
        Thanks for reaching out.  
        Your message landed safely in my inbox and I’ll respond as soon as possible.
      </p>
      <a href="/">Back to Home</a>
    </div>
  </div>

  <div class="right">
    <div class="card">
      <div class="card-content">
        <div class="check">✓</div>
        <h2>Thank You!</h2>
        <p>Your message was successfully delivered.</p>
      </div>
    </div>
  </div>

</body>
</html>
